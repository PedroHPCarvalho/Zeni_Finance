[project]
name = "web-django"
version = "0.1.0"
description = "Aplicação web, junta front e back com o django"
authors = [
    {name = "Pedro",email = "pedroh.fokus@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "django (>=5.2.5,<6.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
